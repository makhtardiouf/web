/*
 * Shuffle uint N, and print the count of numbers similar to it
 * Numbers with leading 0 are not counted
 * Use permutations
 * Makhtar Diouf
*/

var debugOn = true;

function solution(N) {
   if(N == 0)
    return 1;



}

function logMsg(msg) {
    if (debugOn)
        console.log(msg);
}
